{
  "tasks": {
    "start": "deno run --allow-net --import-map=import_map.json src/mod.ts",
    "watch": "deno run --allow-net --import-map=import_map.json --watch src/mod.ts",
    "check": "deno fmt --check && deno lint",
    "test": "deno test --allow-net --import-map=import_map.json"
  }
}
